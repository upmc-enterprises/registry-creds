apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "registry-creds.fullname" . }}-test-connection"
  labels:
    {{- include "registry-creds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['echo']
      args: ['test?']
  restartPolicy: Never
